# Optional: Supabase (free) for saving roster to database.
# Copy to .env and fill in. Get values from https://supabase.com/dashboard → your project → Settings → API.
VITE_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-public-key

# Optional: App access (single user). If not set, defaults: partap.singh / Germany1234
# VITE_APP_USERNAME=partap.singh
# VITE_APP_PASSWORD=Germany1234

# Optional: Backend (NestJS + Redis) for users and roster. When set, top bar shows Login/Register.
VITE_API_URL=http://localhost:3001
